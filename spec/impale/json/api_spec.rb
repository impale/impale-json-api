require 'spec_helper'

describe Impale::Json::Api do
  it 'has a version number' do
    expect(Impale::Json::Api::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
